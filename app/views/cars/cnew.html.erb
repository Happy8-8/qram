<div class="booyah-box col-10 offset-1">
  <h1>Enter Reviews of the Car</h1>
  <%= simple_form_for @car do |f| %>
    <%= f.input :name %>
    <%= f.input :cdescription %>
    <br />
    <%= f.submit 'Create', class: 'btn btn-primary' %>
  <% end %>
</div>